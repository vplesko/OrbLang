// some comment

i64 _a;

fnc inc() i64;

fnc inc(i64 x) i64 {
    ret x+1;
}

fnc inc() i64 {
    _a = _a + 1;
    ret _a;
}

fnc main() i64 {
    1;
    2+3;
    4+5*6-7;
    i64 n=8;
    i64 a=9,b=a+10;
    a=b-11;
    n=a=b=12;
    i64 c;
    c=13;

    {
        i64 f = 14;
        f=a+15;
    }

    {
        i64 a;
    }

    {
        i64 g=16;
        {
            i64 h = 17;
            g=h;
        }
        i64 h;
    }

    inc();
    inc(n);
    inc(1);
    c=inc(c)+2;

    ret 0;
}

i64 g;
i64 i;

fnc foo() {}
fnc foo();

fnc bar(i64 d);
fnc zee(i64 a, i64 e) i64;

fnc eta() i64 {
    ret 18*g;
}

fnc tet(i64 i) {
    g=19;
    i=g+20;
    i64 j=zee(i, inc());
    ret;
}

fnc comment_test() {
    // test
    i64 x = 0; // abcd
    i64 y = 1;//
    i64 z = 2 //
    + 3;
    i64 a; // abcd // pqr
    // lmnop

    /*
    blah
    */

    /**
    blah
    //
    ///
    *
    **/

    /*/*/

    /**/

    i64 b = 1/*blah*/+2;

    ;;;;
}

// end