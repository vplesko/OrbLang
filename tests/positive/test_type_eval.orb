import "util/io.orb";

# TODO!! sym (c);
fnc main () () {
    eval (sym (ty0 i32));
    eval (sym (a:ty0 100));
    println_i32 (eval a);

    # TODO!!
    #eval (sym (id0 \i32));
    #eval (sym (b:((id0)) 101));
    #println_i32 (eval b);

    eval (sym (id1 \cn));
    eval (sym (c:(i32 id1) 102));
    println_i32 (eval c);

    eval (sym (l0 4));
    eval (sym d:(i32 l0));
    eval (= ([] d 0) 103);
    println_i32 (eval ([] d 0));

    eval (sym (ty1 bool));
    eval (sym (e:(i32 ty1) (104 false)));
    println_i32 (eval (. e 0));
};