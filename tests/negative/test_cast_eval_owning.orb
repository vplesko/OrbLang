data Foo;

eval (fnc dropFoo (val:Foo::noDrop) ());

data Foo {
    x:i32
}::((drop dropFoo));

eval (fnc dropFoo (val:Foo::noDrop) () {});

fnc main () () {
    eval (sym f:Foo);
    eval (cast (Foo cn) f);
};