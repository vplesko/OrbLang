import "base.orb";
import "clib.orb";

fnc main () () {
    range i 1 100 {
        if (== (% i 15) 0) {
            printf "FizzBuzz\n";
            continue;
        };
        if (== (% i 3) 0) {
            printf "Fizz\n";
            continue;
        };
        if (== (% i 5) 0) {
            printf "Buzz\n";
            continue;
        };
        printf "%d\n" i;
    };
};