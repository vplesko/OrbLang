import "base.orb";
import "clib.orb";

eval (mac sayHellos (cnt) {
    sym (r {});
    times cnt {
        = r (+ r \{ printf "Hello!\n"; });
    };
    ret \(block ,r);
});

fnc main () () {
    sayHellos 100;
};